# Caesar's Calendar solver

Two useful functions:

- `count_ways` returns the number of ways (not including re-orderings of other ways) to solve the puzzle in the given configuration
- `count_all` computes the above for all possible configurations (combinations of weekday, day and month) and saves the results to `log.csv`
